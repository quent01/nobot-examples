const {projectId, startDate, endDate} = require('./data/example-config.json');

console.log(`project id : ${projectId}`);
console.log(`start date : ${startDate}`);
console.log(`end date : ${endDate}`);